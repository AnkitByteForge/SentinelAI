# Test/lint tooling — not installed in the production image (see .dockerignore
# and the multi-stage Dockerfile, which only installs requirements.txt).
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
fakeredis==2.26.2
testcontainers[postgres]==4.9.1
locust==2.32.6
ruff==0.9.3
